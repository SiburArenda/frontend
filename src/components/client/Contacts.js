import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Contacts extends Component {

    render() {
        return (
            <div>
                Здесь контакты с оф. сайта
            </div>
        );
    }
}

Contacts.propTypes = {};

export default Contacts;